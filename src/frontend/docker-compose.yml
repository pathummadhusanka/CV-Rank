services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cv-rank-frontend
    ports:
      - "3000:80"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped

